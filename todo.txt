- implement imgui for variable sets
- serialize variable sets
- serialize instances and index of variable set used. 

// then
- get reference to graph by type (builder)
- call function on another graph